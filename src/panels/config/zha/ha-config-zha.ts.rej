diff a/src/panels/config/zha/ha-config-zha.ts b/src/panels/config/zha/ha-config-zha.ts	(rejected hunks)
@@ -100,4 +100,3 @@ export class HaConfigZha extends LitElement {
-      this._bindableDevices = (await fetchBindableDevices(
-        this.hass,
-        this._selectedDevice!.ieee
-      )).sort(sortZHADevices);
+      this._bindableDevices = (
+        await fetchBindableDevices(this.hass, this._selectedDevice!.ieee)
+      ).sort(sortZHADevices);
